# Sekrety i dane wrażliwe
.env
.env.*
!.env.example
config/patryk_identity.json
memory/

# Bazy danych i logi runtime
data/
*.db
*.db.bak
cost_log.jsonl
events.jsonl
data/benchmark_results.json
data/.fuse_hidden*

# Artefakty budowania i pakowania
dist/
*.tar.gz
build/

# Python cache i środowiska
__pycache__/
*.py[cod]
*.pyc
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.pdm.toml
uv.lock

# Node.js / Tauri / Frontend
**/node_modules/
**/dist/
**/.vite/
src/src-tauri/target/
src-tauri/target/
**/*.tsbuildinfo

# IDE i systemowe
.idea/
.vscode/
.DS_Store
Thumbs.db

# Lokalne narzędzia AI i workspace'y
.claude/
.grok/
_tools/
personal_v1/
marketing/
SOP-LinkedIn-Sales-Channel.md

# Duże dokumenty binarne (zachowujemy tylko te niezbędne)
docs/Architekt_Wolnosci_Specyfikacja_Pelna_v3.3.pdf

# Paczka beta sponsorowana (generowane lokalnie / w staging)
config/sponsor_payload.py
BETA_SPONSOR.marker

# Tymczasowe pliki testowe i generowane
ab_out/
eval-reviewer.skill
fa-lead-finder.skill
.coverage.*
coverage_baseline.txt
/final_extended.mp4

# Media robocze (reele, VO, materiały FCP) — nie wersjonujemy binarek
*.mp4
*.mp3
*.mov
*.wav
tools/szow_voice_candidates/
tools/fcp-*/

# Artefakty builda paczki desktop (review 2026-07-16) — NIGDY do repo:
# venv PyInstallera i zbudowane binarki sidecara (setki MB, per-maszyna).
.venv-sidecar/
src/src-tauri/binaries/
# Stempel builda sidecara — generowany przez scripts/build-backend-sidecar.sh
# razem z binarką, per-maszyna, nie wersjonowany (jak sama binarka).
BUILD_STAMP

# Katalogi robocze / eksperymenty POZA kodem produktu (review 2026-07-30).
# Zostają na dysku, ale `git status` ma pokazywać kod, nie warsztat — inaczej
# realny nowy plik (jak config/pricing.py) ginie w 27 wpisach untracked.
# assets/ (202 MB) i wieza-rady/ (505 MB) to w sumie 0,7 GB binarek, do tego
# nic w kodzie ich nie importuje — do repo nie mają po co trafiać.
assets/
landing/
polifonia/
widmo/
wieza-rady/
reels/
obsydianowa-rada.html
