> ⚠️ **SUPERSEDED (2026-06-17).** Ten plan zakłada model SaaS/LemonSqueezy i traktuje S0 (domena/support) jako otwarte — nieaktualne. Aktualna roadmapa: **[`docs/roadmap/ROADMAP_2026-06-17.md`](roadmap/ROADMAP_2026-06-17.md)** (model pudełkowy local-first + dual-track, format Now/Next/Later). Zachowany jako snapshot historyczny.

# Plan 100 / 100 — Deployment-ready + Sales-ready (BYOK)

**Cel:** doprowadzić Architekta Wolności do **DEPLOYMENT-READY 100%** i **SALES-READY (BYOK) 100%**, domykając przy okazji **PRODUCTION 88%→100%**.
**Model:** BYOK founders teraz; Hosted SaaS = **Faza 2** (osobny scope, nie blokuje).
**Źródło prawdy:** `AUDIT_PRODUCTION_READINESS.md` (2026-06-03). Wizualizacja: `docs/roadmap/ROADMAP_100.pdf`.
**Stan wejścia:** 572 testy PASS · coverage 80% · 0 failed. P0 bezpieczeństwa **zamknięte**.

Legenda właściciela: **[P]** = Patryk (decyzja/konto, nie da się zdelegować) · **[C]** = kod/konfig (do zrobienia w repo) · **[V]** = weryfikacja.

---

## ŚCIEŻKA KRYTYCZNA (kolejność ma znaczenie)

1. **[P] S0** — domena `mypolyphony.com` + skrzynka `support@mypolyphony.com` + konto LemonSqueezy. *(odblokowuje wszystko poniżej w torze Sales)*
2. **[P] Cena** — akceptacja €149 (patrz `PRICING_PROPOSAL.md`).
3. **[P/C] Privacy URL** — hosting strony polityki → `VITE_PRIVACY_URL`.
4. **[C] CD** — sekrety `REGISTRY_*` + push obrazu na tag.
5. **[C] Tauri** — updater ON + notaryzacja (podpisane buildy).
6. **[V] Sign-off** — ręczny przebieg: pack → install → rejestracja → eksport.

**Najmniejszy następny ruch (≤60 min):** załóż domenę + skrzynkę `support@`. Bez tego S0 stoi, a S0 gatuje cały tor Sales.

---

## TOR A — PRODUCTION (88% → 100%)

> P0 zamknięte (auth fail-closed, RLS na tabelach tenantów, legacy-key odrzucony gdy JWT on). Zostają P1/P2 podnoszące odporność na publicznym hoście.

| ID | Zadanie | Właściciel | Done = | Ryzyko izolacji jeśli pominąć |
|----|---------|-----------|--------|-------------------------------|
| **P1-A2** | BFF + httpOnly cookies dla JWT (zdjąć token z `sessionStorage`/`localStorage`) — `src/src/lib/tokenStorage.ts` | [C] | JWT nieosiągalny z JS przeglądarki; XSS nie wykrada tokenu | Token w storage → XSS = przejęcie sesji tenanta |
| **P1-A5** | Wymuś JWT na publicznym hoście prod — `src/src/App.tsx:47–52,85–88` (app nie wpuszcza bez JWT gdy host ≠ lokalny) | [C] | Na publicznym buildzie brak JWT = brak dostępu | Bez tego `aw_jwt_enabled≠"1"` wpuszcza jako `default` tenant → leak |
| **P1-A3** | Test ścieżki prod: JTI-revoke **bez Redis** musi blokować (fail-closed) — `api/auth_identity.py:14–42` | [C][V] | Test `AW_ENV=production` + brak Redis → 401/block | Cichy fail-open = nieodwoływalne tokeny |
| **P1-B1** | Idempotency-Key / debounce dla startu debaty (retry SSE przed 1. eventem) — `src/src/hooks/useDebate.ts:190–206` | [C] | Podwójny submit nie tworzy 2 debat | Podwójne obciążenie kosztów + duplikaty |
| P2-A1 | CSP prod bez `unsafe-inline` (skrypty) — `tauri.conf.json:26` + `sync-tauri-csp.mjs` | [C] | Build prod ma CSP bez `unsafe-inline` w `script-src` | Osłabiona ochrona przed inject w WebView |
| P2-A2 | Plan usunięcia legacy `ARCHITEKT_API_KEY` — `api/http_guard.py:165–185` | [C] | Decyzja + data deprecacji w docs | — (już odrzucany gdy JWT on) |

**Brama A (100%):** P1-A2, P1-A5, P1-A3, P1-B1 zamknięte i pokryte testem. P2 = polish, nie blokuje.

> ⚠️ Uwaga izolacji do każdej zmiany w tym torze: czy zmiana zakłada poprawny `tenant_id` w ContextVar? Co się dzieje na ścieżce legacy-key i przy braku sekretów? Każdy PR w auth/SSE/completion **musi** odpowiedzieć w opisie.

---

## TOR B — DEPLOYMENT (76% → 100%)

> Ścieżka ręczna (managed PG+Redis+Docker) jest gotowa. Brakuje automatyzacji release i hardeningu desktop.

| ID | Zadanie | Właściciel | Done = |
|----|---------|-----------|--------|
| ~~**P1-D2**~~ | **ZAMKNIĘTE (kod)** — `deploy.yml` trigger `on: push: tags: v*` → auto build+push (+`latest`); bez secrets push pomijany (warning, nie błąd). Doc w `PRODUCTION_CHECKLIST.md` | [C] | ✅ |
| **P1-D2.1** | Sekrety `REGISTRY_USER` / `REGISTRY_TOKEN` / `REGISTRY_URL` w GitHub | [P] | Sekrety ustawione, deploy zielony |
| **P1-D3** | Tauri updater ON (`createUpdaterArtifacts: true`) + endpoint aktualizacji | [C] | `tauri.conf.json` generuje artefakty update |
| **P1-D3.1** | Notaryzacja: Apple Developer ID + Windows signing cert w CI secrets — `tauri-release.yml` | [P][C] | Podpisany `.dmg`/`.msi` przechodzi Gatekeeper/SmartScreen |
| ~~**P1-D1**~~ | **ZAMKNIĘTE** — ping Redis w `/health/ready` (503 gdy `redis_required_in_prod()` a Redis down); dev/demo/SQLite bez zmian; 3 testy | [C][V] | ✅ |
| P2-D1 | Runbook down-migration (pg_dump rollback) — brak migracji „down" | [C] | `docs/` ma procedurę rollbacku |
| P2-D2 | `.env.example` uzupełniony (Sentry, LOG_FORMAT, VITE_PRIVACY_URL, REGISTRY_*) | [C] | Wszystkie prod-ENV opisane |

**Brama B (100%):** P1-D1, P1-D2(+secrets), P1-D3(+signing) zamknięte. Po tagu powstaje podpisany desktop build i obraz serwera bez kroków ręcznych.

---

## TOR C — SALES · BYOK (kod 100% → realnie 100%)

> Kod jest gotowy (RODO UI `AccountPrivacyPanel`, demo guards, pack-scripts). Bloker = decyzje S0 + 2 konta + strona prywatności. To **jedyne** co dzieli Cię od ogłoszenia 100%.

### S0 — Decyzje GTM *(wszystko [P], poza wpisaniem do plików)*
- [ ] **S0.1 Cena** — akceptuj €149 (`PRICING_PROPOSAL.md`) → wpisz do `PRICING.md`, `FOUNDERS_OFFER.md`, `GTM_DECISIONS.md`.
- [ ] **S0.3 Płatność** — LemonSqueezy: utwórz produkt (one-time €149), włącz MoR, wygeneruj checkout link.
- [ ] **S0.4 Support** — `support@mypolyphony.com` → wpisz do `SUPPORT_PLAYBOOK.md`, `FOUNDERS_OFFER.md`, `GTM_DECISIONS.md`.
- [ ] **S0.6 Privacy URL** — opublikuj politykę (patrz S2) → `VITE_PRIVACY_URL` w buildzie/`.env`.

### S2 — Dokumentacja prawna
- [ ] **[C]** `COMPLIANCE_PRIVACY.md` — wypełnić Model A (BYOK) pod realny adres/jurysdykcję (jest szkielet sekcji A).
- [ ] **[P/C]** Hosting strony polityki prywatności (np. prosta strona statyczna na domenie) → URL do `VITE_PRIVACY_URL`.

### S4 — UI / prod marketingowy
- [ ] **[C]** Ukryj „Skip login" w buildzie marketingowym — `LoginScreen.tsx:98–101,228` (P2-E1). Done = publiczny build nie pokazuje wejścia bez konta.
- [ ] **[V]** Demo: panel Prywatność bez akcji eksport/usuń (już zaimplementowane — potwierdź).

### S6 — Sign-off (po S0–S4)
- [ ] **[V]** `pytest tests/ -q` → 0 failed.
- [ ] **[V]** `npm run test:unit` → green (w tym `AccountPrivacyPanel`).
- [ ] **[V]** Ręczny przebieg: `pack-founders-archive.sh` → `unpack` w czystym katalogu → INSTALL → rejestracja → eksport JSON → usuń konto.
- [ ] **[V]** `./scripts/smoke_week1.sh` → exit 0.
- [ ] Zaktualizuj `AUDIT_PRODUCTION_READINESS.md` → SALES-READY BYOK 100% z datą i git hash.

**Brama C (100%):** zero `[DO UZUPEŁNIENIA]` w `docs/FOUNDERS_OFFER.md`, `GTM_DECISIONS.md`, `PRICING.md`, `SUPPORT_PLAYBOOK.md`; przebieg S6 zielony.

---

## FAZA 2 — Hosted SaaS (ODROCZONA, nie blokuje)

Świadomie poza zakresem 100/100 BYOK. Wymaga osobnej decyzji po pierwszych klientach:

- **Stripe / plany** — monetyzacja per-user (dziś: brak; budżet jest globalny per operator).
- **Per-tenant metering** — `cost_log.jsonl` bez `tenant_id` (`core/cost_tracking.py:215–234`); dodać tenant do wpisu.
- **UI RODO self-serve** — dla SaaS; w BYOK panel lokalny wystarcza.
- **Polityka prywatności operatora** — pełna, nie szkielet BYOK.

Ryzyko izolacji Fazy 2: dopiero przy multi-tenant hostingu per-tenant billing i polityka stają się **wymagane**; do tego czasu nie buduj ich „na zapas" — zwiększają złożoność warstwy auth, która ma być prosta.

---

## Mapa „co blokuje co"

| | PRODUCTION 100% | DEPLOYMENT 100% | SALES BYOK 100% |
|---|---|---|---|
| P1-A2/A5/A3/B1 | **wymagane** | — | — |
| P1-D1/D2/D3 + secrets | — | **wymagane** | zalecane |
| S0 (cena/płatność/support/privacy) | — | — | **wymagane** |
| Strona polityki prywatności | zalecane | — | **wymagane** |
| Stripe / per-tenant cost | nie | nie | nie (Faza 2) |

---

*Plan zgodny z AKSJOMAT 0: każda zmiana ma służyć żywemu systemowi, nie rozdmuchiwać scope. Domknięcie (AKSJOMAT 2): najmniejszy ruch = domena + skrzynka support@.*
